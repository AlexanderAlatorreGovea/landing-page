const init = () => {
    const container = document.querySelector(".jumbo-slider");
    const slides = document.querySelector(".jumbo-slider__slide");
    const circles = document.querySelector(".jumbo-slider__circle");
    const links = document.querySelector(".jumbo-slider__links");

    let current = 1;  
    let time = 6000;

    slides.classList.add("jumbo-slider__slide--active")

    links[current -1].classList.add("")
}